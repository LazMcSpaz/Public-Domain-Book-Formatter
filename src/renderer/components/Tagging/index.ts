/**
 * Tagging components barrel (SPEC §5). The integrator mounts `StructurePanel`
 * in the review sidebar; `TagContextMenu` is hosted inside the OutputPane.
 */
export { TagContextMenu } from './TagContextMenu'
export type { TagContextMenuProps } from './TagContextMenu'
export { StructurePanel } from './StructurePanel'
