You are correcting a machine reading of five leaves of H. P. Blavatsky's _The Secret Doctrine_, Vol. II, _Anthropogenesis_ (London: The Theosophical Publishing Company, 1888), from Theosophical University Press's online edition of its photographic facsimile.

**Read this first, it is the whole job.** There is **no picture of these leaves anywhere**. The files were put through Acrobat ClearScan, which OCR'd the scan, drew the text again in a synthesised font and threw the photograph away. So you are given **two machine readings of the same leaf** and nothing else, and your job is: _where do they differ, and which of the two does the book itself say is right?_ You never write a word neither reading has (the exceptions are listed under Rules), and you never correct the author.

## Inputs

Brief: `BRIEF` — an array, one entry per leaf:

- `leaf`; `blocks` `[{i, kind, text}]` (keep `i`) — **the first reading**: ClearScan's own text layer, shaped into blocks from where ClearScan placed each line, with the conversion's mechanical damage already taken off (TUP's "Online Edition" stamp removed, words the layer split with spaces joined where the second reading sets them whole, quotes curled, the space before `, . ; : ! ?` and inside quotation marks closed, `•` and `t` at a note's head set as `*` and `†`);
- `secondReader` — **the second reading**: Tesseract run over the page ClearScan draws, as one run of text with the running head at its front. It has whole words and line-end hyphens (`con- ditioned`), and its own faults: whole passages with `v` for `r` (`De Vivibus Membrovum`), `7.¢.` for `i.e.`, `Brahm4` for `Brahmâ`, `oF`/`Tue`/`SuMMING` for small capitals, quotation marks read as `‘“`, `«“`, `**`, `*’`, and a footnote mark it reads as `*` or drops;
- `disagreements` — the places the two readings' **words** differ (punctuation is not compared; read it yourself).

Use `WORKDIR` for scratch files. Do not write anywhere else.

**The book's own vocabulary is your witness.** Count a word before you rely on it (run from `/home/user/Public-Domain-Book-Formatter`, with `SD_KIT=KITDIR`):

```bash
python3 scripts/reading-kit/sd/lex.py Mulaprakriti "Mula prakriti" origo ovigo   # counts in Vol. I and Vol. II
python3 scripts/reading-kit/sd/lex.py --ctx "tc be" 5                             # where this volume sets a phrase, by leaf
```

## How to decide a disagreement

1. **One reading is a word and the other is not**: take the word. `origo`/`ovigo`, `term`/`tern`, `Subject`/`Snbject`. An ordinary English word needs no count; a name, a Sanskrit or technical term, or a Latin phrase does — take the form the book sets, by `lex.py`.
2. **Both are words** (`arc`/`are`, `form`/`from`, `Brahma`/`Brahmâ`): the sentence usually settles it — read the whole block and the ones either side. If it truly does not, keep the first reading and raise `unclear`, naming both, no fix.
3. **Neither is right, or neither is a word** (Greek, Hebrew, Devanagari, a mangled name the book never sets again): keep the first reading exactly and raise `unclear` once per passage, saying what is lost. Never reconstruct Greek or Sanskrit from memory.
4. **Both readings agree on something plainly wrong** (`tc be`, `Egyptain`): ClearScan built its outline from the paper, so this is most likely what 1888 printed. Transcribe it as both give it and raise `printers-error` with the `fix` (the shelf's standing ruling 1 corrects this class; propose a fix only where it is certain).
5. **Accents and ligatures** (the editor's standing ruling for this volume: an accent is set from a reading's trace, and the ligature is the proper spelling): neither reading carries them as characters, and `lex.py` counts accent-free text, so it cannot confirm one. Each reading leaves a **trace** instead, and a trace is evidence. Set the accented letter wherever either reading shows one of these traces at that place, and set the bare letter where neither does:
   - `â`: second reading `4`, `d`, `&`, `é`, `i`/`ia` (`Brahm4`, `Purdna`, `Jaganniatha`); first reading `tl`, `tt`, `il`, `;l`, `d`, and `n1` for `râ` (`Brahmtl`, `Brahmd`, `Pun1nas`); the second reading's `i` for `â` mid-word as well as at the end (`Varidha`, `Brahmi`).
   - `ü`: `ii` or a dotted `i.i` (`Miiller`, `Mi.iller`) → `Müller`.
   - `æ`/`Æ`: first reading `re`, `<e`, `;:e`, `/E`, `JE`, `.iE`, `ce`, `ao` (`Chaldaoan`); second `z`, `Z`, `@`, `ae` → `æ`/`Æ` (`archæology`, `Æther`, `æons`).
   - `œ`/`Œ`: first reading `ce`, `re`, `fw`, `£ce`, second `oe` or `e` → `œ` (`fœtus`, `Phœnician`, `Œdipus`).
   - `ô`, `é`: `6`, `»é` (`r6le` → `rôle`).
   - **A split in the first reading**: ClearScan breaks a word with a space exactly where an accented letter sits (`Brahm a`, `M uller`, `Sankarach arya`, `Kashin ath`). That split is a trace too, even where the second reading shows none (it reads Müller as plain `Miller` throughout). A split **right after a word's capital** is not a trace: ClearScan splits there with no accent anywhere (`B uddhas`, `M anushi`, `N OUME NO N`). Only a split inside the word, at a vowel, counts.
     Do **not** normalise a word the book sets two ways across leaves; that is done later, by count, across the whole book.
6. **Words run together**: where both readings run two ordinary words together (`ofthe`, `monopolisethe`, `copperandgold`), the word space was lost in conversion, not by the compositor: put it back, with no query. A printer's error is a wrong letter, not a lost space.
7. **Punctuation**: where one reading has a stop the sentence needs and the other has none (`could ensue` / `could ensue.`), take the stop. Where both construe, keep the first reading.

## What these leaves are

- **Running heads and folios** often sit at the front of block 0 rather than in a block of their own — check its first words. They are `THE SECRET DOCTRINE.`, `ANTHROPOGENESIS.`, `PRELIMINARY NOTES.`, `CONTENTS.`, a section's own head (`THE ABSOLUTE KNOWS ITSELF NOT.`), and a folio (`55`, `xxiii`). Leave them out: cut them from the text, or set a block holding only one to `""`. They are usually already gone; the second reading always has one at its front.
- **Footnote marks** are `*`, `†`, `‡`, `§`, `‖`, `¶`, doubled on a busy leaf. The first reading gives `*` as `•` or `*`, `†` as `t`, `+` or `f`, `‡` as `:!:`, `t` or `I`. Part II adds more: `‘:’`, `’:’` and a trailing `‘’` after a word are a `*`; `1:`, `11`, `!!` and `II` are `‡` or `‖`, and `‘IT` is `¶` — take which from the order the marks run in and the heads of the notes at the foot. The second reading gives `‡` as `{`, `}`, `|` or `1`. **A `§` is also a section sign** (`§ IV.` in a heading, `vide § VII.`, `preceding §`): leave it as printed and list each one in your report, because only the notes at the foot tell a section sign from a mark. A note stands at the foot of the leaf with its mark at its head, **one note to a block**: where the draft ran two or three notes into one footnote block, split them (a new block for each, with no `i`), because a block holding two notes gives the second one no mark to claim. A note that **runs over** from the previous leaf has no mark on this leaf — its first block is a `footnote` beginning mid-sentence; do not give it one.
- **Letter-spaced heads** come through as single letters (`S T A N Z A`, `P RO E M`): set them as words.
- **The Stanzas** of the Book of Dzyan are set in capitals, each sloka numbered (`1. THE ETERNAL PARENT WRAPPED IN HER EVER INVISIBLE ROBES HAD SLUMBERED …`). Keep the capitals; each sloka is its own `blockquote`, and the commentary after it is `paragraph`. The draft types the Stanza heads and `COMMENTARY.` as `blockquote` and the slokas as `paragraph`: retype them on every Stanza leaf (a Stanza head is `heading` level 2, `COMMENTARY.` level 3).
- **Small capitals** come through mixed case (`DocTRINE`, `ABsOLUTE`): set them in full capitals.
- **Figures**: the first reading gives `r` for 1, `o` for 0, `s` for 5, `S` for 8, spaced apart (`r o,sSo`); the second reading's numbers are nearly always right. `\V` is `W`; `Y` inside a word is `v`.
- **Italics are lost in both readings**. Do not add `<i>` anywhere; nothing here can say where it went.
- **Line-end hyphens** the layer kept (`pre- Cosmic`, `Ex-istence`, `imperme- able`): the second reading shows whether the page broke a line there (`imperme- able` at a line end) — join a word that is only broken; keep a hyphen in a real compound, and use `lex.py` when unsure (where the book sets both the hyphened and the solid form, keep the one this leaf's second reading gives).
- **The contents pages** (in the front matter, leaves 4–13) are replaced in this edition by one built from the text; read them as they are, but do not spend effort on dotted leaders or page numbers — leave those as the first reading has them.
- The editor's decisions that govern every book here are in `/home/user/Public-Domain-Books-Storage/RULINGS.md`. Read rulings 1–4 once before your first query.

## What the first stretch of this volume found (leaves 0–69)

- **More accent traces**: the first reading's `ft` and `’l` (`Brahmft`, `Brahm’lns`) are `â`; `U!`, `od` are `œ` (`FU!tus`, `Codus`); `<e` is `æ` (`Astr<ea`). The second reading's `z` mid-word is `æ` (`Astrza`, `Palzolithic`), `ee` is `œ` (`Feetus`), `oe` is `œ`, and a mid-word capital `A` (`MahabhAdrata`) or `i` (`Purinas`, `Sivd`) is `â`. A `�` (U+FFFD) inside a name in the first reading is an accent trace; one standing alone is a mark or a lost glyph the second reading usually names. A `é` in the second reading over a plain `e` (`preéxisted`, `Vaégo`) is a diaeresis or an accent the list does not cover: leave the letter bare and raise `unclear`.
- **Marks**: the first reading's `‘:’`, `’:’`, `.;’`, `’‘` and a trailing `‘ ’` after a word are a `*`; ` t`, `,t`, `+` are `†`; `!` at a sentence's end where a third note stands at the foot is `‡`. The second reading gives `*` as `%`, `”`, `®` or nothing, `†` as `+`/`t`/`1`, `‡` as `}`/`{`/`|`. **A `§` in running text is nearly always a section sign** (`Part II., § “Adam-Adami”`, `vide § VII.`) and a `§ (5)` opening a Stanza's synopsis is one too: list every `§` in your report, because an unclaimed one steals the next `§` note in the book.
- **Lost glyphs**: the Sephiroth diagram, π, Hebrew words, Devanagari, the planetary and geometrical symbols (`@`, `C])`, `t*`, `8`, `9f`, `:fJ`) come through both readings as residue. Keep the first reading's residue exactly and raise `unclear` once per passage. A `*` inside such residue is not a mark; say so.
- **Stanza leaves**: each opens with a small-type synopsis (`§§ (1) … (2) …`) that the draft types `footnote`: it is `blockquote`. A Stanza head (`IV.`, `V.`) is merged into the front of the first sloka or arrives as a one-cell `table`: pull it out as `heading` level 2. The first reading letter-spaces every small-capital word on these leaves (`T H EM S E LV E S`, `THEM ANASA` = THE MANASA), so a split between capitals there is never an accent trace. Ellipses inside slokas come as `• • • •` or `. ., ,`: set spaced points `. . . .`. The commentary's copy of each sloka, later in the volume (`lex.py --ctx`), is the best witness for its punctuation.
- **Small capitals**: `LoGos`, `Locos` (both readings) are LOGOS; `Lorp`, `Wispom`, `HicH`, `Worp` in the second reading are LORD, WISDOM, HIGH, WORD. `B. C.`/`A. D.` are set `B.C.`/`A.D.`.
- **Figures**: in the first reading `r`=1, `o`=0, `s`=5, `S`=8, `z`=2, `g`=9, `I`/`J`=1, spaced apart (`2o6 r 2`); the second reading's figures are right in the body and wrong in the contents. Where both give `o` inside a figure it is still `0`.
- **Compounds**: ClearScan drops a real hyphen at a line end and leaves a space (`Sun gods`, `Venus Lucifer`), so a space in the first reading where the second reading breaks a line at a hyphen is a compound to count with `lex.py`.
- **Lost and shifted word spaces** (`Isleof`, `black,s warthy`, `Thisrelates`) are the conversion's: put them back with no query.
- **From leaves 70–134**: the first reading's `ri`, `il`, `lui`, `/i` and `ntn` are â traces (`Mahdbhrirata`, `Brahmil`, `Pramantlui`, `Pur/ina`, `P untnas`); its `<;`/`<;:` is `ç`; `tE`, `.‘E`, `iE`, `a=`, `m` and `tT` at an æ are æ residue (`JudtEo`, `D.‘EMON`, `iETERNA`, `H a=ckel`, `Phmdrus`, `TimtTus`); `\V`, `\iV`, `::\1` are `W`, `W`, `M`; `!` at a word's head is `I`, `Q` is `(J`; `Luers` is LUCIS. A `t` glued to the word before a bracket or comma (`Demonst`, `treet`) is `†` or `‡` when the foot has that note. The first reading loses an em dash after a closing quotation mark or at a line end; the second reading keeps it. The second reading's `é` mid-name (`Kuméras`, `Brahmé`, `Purénic`) is `â` where the same name shows `d`/`4`/`i` nearby; its `d` inserted after a vowel (`Kumadras`, `versd`) is â too; it turns a final `h` to `k` in Hebrew names, `J` to `F`, `n` to `w`, and gives `{`/`}` for `‡` even on a leaf with only two notes — count the notes at the foot first. Small capitals in running text come through the first reading letter-split (`RoMAKUPAS`), never an accent trace. A body paragraph's last lines above a note runover are typed `footnote` by the draft: check what the previous leaf's last block continues. A `(Ibid.)` floating between notes is the tail of the note above. Tables of figures (yugas, elements) arrive as prose blocks: describe the columns in your report, one row per line, and do not restructure.
- **From leaves 135–204**: ClearScan drops the `fi` ligature outright (`the rst androgyne` in both readings): raise it `printers-error` with the fix. `Hæckel` is the form throughout (first reading `H <eckel`, `H::eckel`, `Ha:ckel`; second `Heckel`/`Haeckel`); `Moneron`, never `Moneran`. The first reading's `rrui`, `ui`, `fm`, `:‘`, `ni`, `!` and `A` at the vowel are â traces (`Brahrrui`, `Kzmuiras`, `Purfmas`, `Pur:‘mas`, `Vaniha`, `Pur!nas`, `PurAnas`); its `a;`, `m`, `a:`, `he`, `ce` (in a Latin genitive) are æ (`pala;onto`, `coryphmi`, `Ha:ckel`, `paheozoic`, `substantice`); `ce` in a Greek name is œ (`Kœus`); `i·` at a note's head is `†`, `j:` is `‡`, `.; :` is `‖`, a lone `�` at a sentence end is `‡` and `��` is `¶`; `£` mid-word is a lost italic `i`; `lis!.` is `fish`; `Ita` is `Ila`; `ll` comes as `ii` (`wiii`, `stiii`). The second reading gives `f`/`F` for `J` through whole leaves (`Fehovah`, `Fupiter`), `k` for a final `h`, `v` for `r` through every Latin and italic passage, `¢`/`«`/`‘“`/`**` for an opening `“` and `””`/`’’` for a closing one, `{` for `†` as well as `‡`, `;` glued to a stop for `‡`, and `%`/`™` for `*`. A trailing `’‘` or `‘ ’` on the first reading is a **closing `”`** when a quotation is open and a `*` only when the foot has an unclaimed note: check the foot first. The first reading drops ellipses inside quotations that the second keeps; take them. `Swayambhuva` and `Bhûta` carry an accent on the **u** (second reading `ii`/`ti`/`fi` at the u): set `û` there, never `ü`, and say so. Marisha/Mârishâ and Râkshasas get their trace on a different vowel at each setting: set per trace and raise once for the count. A citation both readings give plainly wrong (`p. 777` of a 200-page book) is not settled by their agreement — raise it without a fix. The draft can drop a leaf's notes entirely (183): tokens in `disagreements` that no block holds are the sign; rebuild from the second reading. A long note in several paragraphs (149, 150): join only the line cuts and leave each paragraph its own markerless `footnote` block so the breaks survive.
- **From leaves 205–274**: a word broken at the foot of your last leaf is set whole there only if you have the next leaf; if you do not, say so in your report and leave it as the leaf gives it, because the next leaf's reader will not know to remove its tail. Likewise a leaf that opens with the tail of a broken word (`ing to be`, `ments of`) is the previous leaf's: say so rather than guessing. A footnote the draft typed `paragraph` (a block at the foot opening with a mark) must be retyped `footnote`, or every note after it pairs a leaf early. The first reading's `<B`, `;E` and `<:`/`<E` inside a word are æ; `£ce` at a word's head is fœ; `lt1` is M, `,J` is o, `lz` is h, `11` is n, `Jew` is few, `che` is the. A French word the second reading prints with `é`, `è` or `ê` (Lévi, Halévy, Mémoire) is set as it shows; an é over a Sanskrit or English word is still raised `unclear`.
- **From leaves 275–344**: asterisks standing for omitted words (`* * *`) are not note marks: keep them and name each such place in your report, because every one steals a note. A `printers-error` fix must be the **whole quote corrected**, not the corrected word alone: `"fix": "giving life—he"` for `"quote": "giving lite—he"`, never `"fix": "life"`. The first reading's `m` is often `in`, `o` alone is often `of`, and a doubled `by by` or `the the` is real. An author's own slip in a name or citation (`Stephenson` for Stevenson) is raised `inconsistent` with no fix: ruling 1 makes it a note, not a correction. A running head can arrive as a block typed `heading`: check it against the second reading's front.
- `lex.py` counts Vol. II off its second reading, so a leaf's own reading counts itself once; discount that when judging a majority, and a word OCR garbled reports "not found" even where the leaf sets it.

## Block kinds and heading levels

The draft guessed each block's `kind` from where ClearScan put its lines. Where a kind is plainly wrong, give the right one as `"kind"`: `paragraph`, `heading`, `blockquote`, `footnote` or `caption`. A paragraph's last lines typed `footnote` on a leaf with no notes are `paragraph`; an extract or verse set in small type within the text is `blockquote`; a footnote is only what stands at the foot of the leaf.

**Every heading gets a `"level"`**: `1` for a division of the book (`PRELIMINARY NOTES.`, `PART I. ANTHROPOGENESIS.`, `PART II. THE ARCHAIC SYMBOLISM OF THE WORLD-RELIGIONS.`, `PART III. ADDENDA.`, each with its sub-title), `2` for a Stanza (`STANZA I.`) or a numbered section (`§ VI. THE MUNDANE EGG.`), `3` for a sub-head inside one (`SUMMING UP.`, a side-head). A title set on two lines is two heading blocks at the same level.

## Output

Write `OUT` — shape:

```json
[{"leaf": N, "blocks": [{"i": 0, "text": "...", "kind": "only where the draft's is wrong", "level": 2, "join": true}, ...],
  "add": [{"after": 3, "kind": "footnote", "text": "only for a block the draft left out altogether", "level": "only on a heading"}],
  "queries": [{"quote": "exact words of the first reading", "why": "...", "kind": "printers-error|inconsistent|unclear", "fix": "only for printers-error"}]}]
```

**Write `OUT` after each leaf you finish** (the whole array so far), not only at the end.

Every block, corrected or not, with its `i`. `level` on every heading block, whether or not you changed it.

## Rules

1. **The page's words and spelling as 1888 printed them**, as far as the two readings and the book can show it. The words you may write that neither reading has are only: a word the first reading split, joined; a line-end hyphen healed; a note mark's glyph; and the stop of rule 7 above; the letters of rule 5.
2. **Quotation marks:** the first reading's were curled by machine from straight quotes and are often turned the wrong way or shifted across a word space (`that” these` for `that “these`). Take where each mark stands, and which way it faces, from the second reading and the sense of the quotation. **No space** before `, . ; : ! ?`, none just inside a quotation mark, curly quotes `“ ” ‘ ’`, `—` for a dash (the readings give `--`, `—`, or `-`).
3. **Before raising anything, read the whole block it sits in and the blocks either side**, and search the book (`lex.py --ctx`). A finding the paragraph or the book settles is not a query.
4. **An unclosed or unopened quotation mark** is left as printed and raised `inconsistent` with no fix — the editor's standing ruling is to leave these unpaired. **One term set two ways** is left as each place prints it and raised `inconsistent`; it is settled by count across the book afterwards.
5. What is genuinely the editor's — the book contradicting itself, a sentence that does not construe in either reading — `inconsistent` or `unclear`, transcribed as the first reading has it, **no fix**.
6. A word broken across two blocks **or two leaves** is set whole in the first and removed from the second.
7. **A paragraph or a note the draft cut at a line** into two or more blocks: give each continuation block `"join": true` (never the first part). It is folded into the block before it on landing. If a block holds two paragraphs run together, do not restructure: say so in your report with the leaf and `i`.
8. **The draft types small type as `footnote`**, so an extract or a continuing quotation that runs on in small type near the foot of the leaf arrives as a footnote. A block is a note only when a mark in the text claims it or it is a runover with no mark: retype the rest `blockquote`. And a word both readings agree on is never corrected silently, however plainly wrong (a misquoted title, a misspelt name): it is a `printers-error` with its `fix`.
9. A block typed `table` is regenerated from its cells, so prose written into it is lost: if one is really prose, say so in your report. A table the conversion ran into prose, or whose columns it interleaved, you describe in your report column by column; it is built by hand after landing. Where a brace spans rows, the entry belongs on the first row of its span.

Work leaf by leaf. Report back only: blocks changed per leaf, queries by kind, and anything a later batch should know about either reading that is not already above.
